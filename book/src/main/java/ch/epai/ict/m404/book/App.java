package ch.epai.ict.m404.book;

/**
 * La classe App représente l'application.
 */
public class App {

    /**
     * Point d'entrée du programme. La première instruction du programme est la
     * première instruction de la procédure main (procédure principale). Cette
     * procédure est appelée par l'interpréteur java.
     *
     * @param args paramètres de la ligne de commande
     */
    public static void main(String[] args) {
        new Ctl().start();
    }
}
